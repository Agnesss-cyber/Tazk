import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Descriptive color names based on your hex codes
  static const Color darkGray = Color(0xFF4A4A4A);
  static const Color lightGray = Color(0xFFCBCBCB);
  static const Color offWhite = Color(0xFFFFFFE3);
  static const Color slateBlue = Color(0xFF6D8196);

  // Optional: Semantic names 
  static const Color primaryText = darkGray;
  static const Color secondaryText = lightGray;
  static const Color background = offWhite;
  static const Color primaryAccent = slateBlue;
}
